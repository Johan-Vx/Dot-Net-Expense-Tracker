<UserControl x:Class="Expense_Tracker.View.BudgetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expense_Tracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1C2420" CornerRadius="15" Padding="25" Margin="0,0,0,25" BorderBrush="#2A352F" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Lập Ngân Sách Tháng" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Theo dõi hạn mức chi tiêu của từng tài khoản" FontSize="15" Foreground="#8A9A92" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
            <StackPanel>

                <Border Background="#1C2420" CornerRadius="15" Padding="22" Margin="0,0,0,22" BorderBrush="#2A352F" BorderThickness="1.5">
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="55" Height="55" CornerRadius="27.5" Background="#0054A6">
                                <TextBlock Text="MB" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="18" FontWeight="Bold"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="MB Bank Budget" FontSize="22" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Ngân hàng Quân Đội - Tháng 03/2026" FontSize="14" Foreground="#8A9A92" Margin="0,5,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="3,500,000 / 5,000,000đ" Foreground="White" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                <TextBlock Text="Còn lại: 1,500,000đ" Foreground="#2ED573" FontSize="14" FontWeight="Medium" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <ProgressBar Height="16" Minimum="0" Maximum="100" Value="70" Foreground="#2ED573">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border Background="#2A352F" CornerRadius="8"/>
                                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="8" Background="{TemplateBinding Foreground}"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                        </Grid>
                        <TextBlock Text="70% dung lượng đã sử dụng" FontSize="12" Foreground="#8A9A92" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="#1C2420" CornerRadius="15" Padding="22" Margin="0,0,0,22" BorderBrush="#2A352F" BorderThickness="1.5">
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="55" Height="55" CornerRadius="27.5" Background="#009245">
                                <TextBlock Text="OCB" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="16" FontWeight="Bold"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="OCB Bank Budget" FontSize="22" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="Ngân hàng Phương Đông - Tháng 03/2026" FontSize="14" Foreground="#8A9A92" Margin="0,5,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="2,300,000 / 2,000,000đ" Foreground="White" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                <TextBlock Text="Vượt ngưỡng: 300,000đ" Foreground="#FF4757" FontSize="14" FontWeight="Medium" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <ProgressBar Height="16" Minimum="0" Maximum="100" Value="100" Foreground="#FF4757">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border Background="#2A352F" CornerRadius="8"/>
                                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="8" Background="{TemplateBinding Foreground}"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                        </Grid>
                        <TextBlock Text="115% - Đã vượt hạn mức!" FontSize="12" Foreground="#FF4757" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
