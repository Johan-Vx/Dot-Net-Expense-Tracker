<Window x:Class="Expense_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Expense_Tracker"
        xmlns:vm="clr-namespace:Expense_Tracker.ViewModel"
        xmlns:behavior="clr-namespace:Expense_Tracker.Behavior"
        mc:Ignorable="d"
        Title="Quản lý Quỹ Thu Chi" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ColorBackground}"
        behavior:WindowBehavior.DialogResult="{Binding DialogResult}">
    <!-- Binding DialogResult để hỗ trợ đóng Window từ ViewModel -->

    <!-- Khởi tạo ViewModel hoàn toàn bằng XAML -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource ColorSurface}">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Top" Margin="10,20,10,20">
                    <TextBlock Text="QUẢN LÝ QUỸ" FontSize="20" FontWeight="Bold" Foreground="{StaticResource ColorAccent}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <!-- User Info -->
                    <Border Background="#33334A" CornerRadius="5" Padding="10" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="{Binding CurrentUser.HoTen}" Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentUser.VaiTro}" Foreground="#A0A0B5" FontSize="12" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Navigation Buttons -->
                    <Button Content="📊 Tổng Quan" Command="{Binding NavigateDashBoardCommand}" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📝 Lập Phiếu" Command="{Binding NavigatePhieuThuChiCommand}" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📖 Sổ Quỹ" Command="{Binding NavigateLedgerCommand}" Style="{StaticResource SidebarButtonStyle}"/>

                    <Button Content="⚙️ Cài Đặt" Command="{Binding NavigateSettingsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButtonStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAdmin}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Logout -->
                <Button DockPanel.Dock="Bottom" Content="🚪 Đăng Xuất" Command="{Binding LogoutCommand}" Style="{StaticResource SidebarButtonStyle}" Margin="10,0,10,20"/>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20"/>
    </Grid>
</Window>