<UserControl x:Class="Expense_Tracker.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expense_Tracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cài Đặt Hệ Thống" FontSize="28" FontWeight="Bold" Foreground="{StaticResource ColorTextPrimary}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Làm Mới" Command="{Binding RefreshCommand}" 
                        Background="{StaticResource ColorSurfaceHover}" Foreground="{StaticResource ColorTextSecondary}"
                        BorderThickness="1" BorderBrush="{StaticResource ColorBorder}" Padding="15,8"
                        FontFamily="{StaticResource FontMain}" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <Button Content="Lưu Thay Đổi" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <TabControl Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Padding="15,10" Margin="0,0,5,0" Background="{StaticResource ColorSurfaceHover}" CornerRadius="5,5,0,0">
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ColorAccent}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ColorSurface}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ColorAccentHover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="{StaticResource ColorTextSecondary}"/>
                        <Setter Property="FontFamily" Value="{StaticResource FontMain}"/>
                        <Setter Property="FontSize" Value="14"/>
                    </Style>
                </TabControl.Resources>

                <!-- Danh Mục -->
                <TabItem Header="Danh Mục">
                    <DataGrid ItemsSource="{Binding DanhMucs}" Style="{StaticResource DataGridDarkStyle}" CanUserAddRows="True" CanUserDeleteRows="True" IsReadOnly="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã" Binding="{Binding MaDM}" Width="80"/>
                            <DataGridTextColumn Header="Tên Danh Mục" Binding="{Binding TenDM}" Width="200"/>
                            <DataGridTextColumn Header="Loại" Binding="{Binding LoaiDM}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                
                <!-- Tài Khoản Quỹ -->
                <TabItem Header="Tài Khoản Quỹ">
                    <DataGrid ItemsSource="{Binding TaiKhoanQuys}" Style="{StaticResource DataGridDarkStyle}" CanUserAddRows="True" CanUserDeleteRows="True" IsReadOnly="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Quỹ" Binding="{Binding MaQuy}" Width="80"/>
                            <DataGridTextColumn Header="Tên Quỹ" Binding="{Binding TenQuy}" Width="200"/>
                            <DataGridTextColumn Header="Số Dư Ban Đầu" Binding="{Binding SoDuBanDau}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <!-- Người Dùng -->
                <TabItem Header="Người Dùng">
                    <DataGrid ItemsSource="{Binding NguoiDungs}" Style="{StaticResource DataGridDarkStyle}" CanUserAddRows="True" CanUserDeleteRows="True" IsReadOnly="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã" Binding="{Binding MaND}" IsReadOnly="True" Width="80"/>
                            <DataGridTextColumn Header="Tên Đăng Nhập" Binding="{Binding TenDangNhap}" Width="150"/>
                            <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="200"/>
                            <DataGridTextColumn Header="Mã Vai Trò" Binding="{Binding MaVaiTro}" Width="100"/>
                            <DataGridCheckBoxColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
