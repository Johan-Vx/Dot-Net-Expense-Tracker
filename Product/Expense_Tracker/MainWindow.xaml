<Window x:Class="Expense_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:Expense_Tracker.View"
        xmlns:vm="clr-namespace:Expense_Tracker.ViewModel"
        mc:Ignorable="d"
        Title="Quản lý tài chính" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashBoardViewModel}">
            <v:DashBoardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type v:BudgetsView}">
            <v:BudgetsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TransactionsViewModel}">
            <v:TransactionsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AccoutntsViewModel}">
            <v:AccountsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type v:CategoriesView}">
            <v:CategoriesView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#2C3E50">
            <TextBlock Text="Chào mừng trở lại" Foreground="White" Margin="20" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Button Content="Tổng quan" Command="{Binding ShowDashBoardCommand}" Margin="10" Height="40" Background="#34495E" Foreground="White"/>
            <Button Content="Ngân sách" Command="{Binding ShowBudgetsCommand}" Margin="10" Height="40" Background="#34495E" Foreground="White"/>
            <Button Content="Giao dịch" Command="{Binding ShowTransactionCommand}" Margin="10" Height="40" Background="#34495E" Foreground="White"/>
            <Button Content="Tài khoản" Command="{Binding ShowAccountsCommand}" Margin="10" Height="40" Background="#34495E" Foreground="White"/>
            <Button Content="Danh mục" Command="{Binding ShowCategoriesCommand}" Margin="10" Height="40" Background="#34495E" Foreground="White"/>
        </StackPanel>
        <ContentControl Grid.Column="1" Content="{Binding CurrentChildView,Mode=TwoWay}"/>
    </Grid>
</Window>
