<Window x:Class="Expense_Tracker.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Expense_Tracker.View"
        mc:Ignorable="d"
        Title="Đăng Nhập" Height="400" Width="350"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="{StaticResource ColorSurface}" CornerRadius="10" BorderBrush="{StaticResource ColorAccent}" BorderThickness="1">
        <StackPanel Margin="30">
            <TextBlock Text="🔒" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="ĐĂNG NHẬP" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ColorAccent}" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            
            <TextBlock Text="Tên đăng nhập" Margin="0,0,0,5" Foreground="{StaticResource ColorTextMuted}"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="35"/>
            
            <TextBlock Text="Mật khẩu" Margin="0,0,0,5" Foreground="{StaticResource ColorTextMuted}"/>
            <PasswordBox Name="txtPassword" Margin="0,0,0,20" Height="35"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ColorDanger}" TextWrapping="Wrap" Margin="0,0,0,15" HorizontalAlignment="Center"/>
            
            <Button Content="Đăng Nhập" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,10"/>
            <Button Content="Thoát" Command="{Binding CloseCommand}" Style="{StaticResource DangerButtonStyle}"/>
        </StackPanel>
    </Border>
</Window>
