<Application x:Class="Expense_Tracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Expense_Tracker"
             xmlns:v="clr-namespace:Expense_Tracker.View"
             xmlns:vm="clr-namespace:Expense_Tracker.ViewModel">
    <Application.Resources>
        <!-- ── Design Tokens: Indigo-Slate Light Theme ── -->
        <SolidColorBrush x:Key="ColorBackground" Color="#F8FAFC"/>      <!-- Slate-50  — page background -->
        <SolidColorBrush x:Key="ColorSurface" Color="#FFFFFF"/>          <!-- White    — cards / panels -->
        <SolidColorBrush x:Key="ColorSurfaceHover" Color="#F1F5F9"/>     <!-- Slate-100 — hover / alt rows -->
        <SolidColorBrush x:Key="ColorAccent" Color="#4F46E5"/>           <!-- Indigo-600 — primary accent -->
        <SolidColorBrush x:Key="ColorAccentHover" Color="#4338CA"/>      <!-- Indigo-700 — hover -->
        <SolidColorBrush x:Key="ColorAccentLight" Color="#818CF8"/>      <!-- Indigo-400 — labels / subtle -->
        <SolidColorBrush x:Key="ColorSuccess" Color="#16A34A"/>          <!-- Green-600  — Thu (Income) -->
        <SolidColorBrush x:Key="ColorSuccessMuted" Color="#86EFAC"/>     <!-- Green-300  — charts / labels -->
        <SolidColorBrush x:Key="ColorDanger" Color="#DC2626"/>           <!-- Red-600    — Chi (Expense) -->
        <SolidColorBrush x:Key="ColorDangerMuted" Color="#FCA5A5"/>      <!-- Red-300    — charts / labels -->
        <SolidColorBrush x:Key="ColorWarning" Color="#D97706"/>          <!-- Amber-600  — warnings -->
        <SolidColorBrush x:Key="ColorTextPrimary" Color="#1E293B"/>      <!-- Slate-800  — headings -->
        <SolidColorBrush x:Key="ColorTextSecondary" Color="#475569"/>    <!-- Slate-600  — body text -->
        <SolidColorBrush x:Key="ColorTextMuted" Color="#94A3B8"/>        <!-- Slate-400  — captions -->
        <SolidColorBrush x:Key="ColorBorder" Color="#E2E8F0"/>           <!-- Slate-200  — borders -->

        <!-- Sidebar-only dark tokens -->
        <SolidColorBrush x:Key="SidebarBackground" Color="#1E293B"/>     <!-- Slate-800 -->
        <SolidColorBrush x:Key="SidebarAccent" Color="#312E81"/>         <!-- Indigo-900 -->
        
        <CornerRadius x:Key="RadiusCard">10</CornerRadius>
        <FontFamily x:Key="FontMain">Segoe UI</FontFamily>

        <!-- Base TextBlock Style -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource FontMain}"/>
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}"/>
        </Style>

        <!-- Global Button Helper: Prevents implicit TextBlock style from making button texts dark -->
        <Style TargetType="Button">
            <Style.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                </Style>
            </Style.Resources>
        </Style>

        <!-- Data Templates for Navigation -->
        <DataTemplate DataType="{x:Type vm:DashBoardViewModel}">
            <v:DashBoardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PhieuThuChiViewModel}">
            <v:PhieuThuChiView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LedgerViewModel}">
            <v:LedgerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatisticViewModel}">
            <v:StatisticView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ReportViewModel}">
            <v:Report/>
        </DataTemplate>

        <!-- SidebarButtonStyle (stays light-on-dark) -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontFamily" Value="{StaticResource FontMain}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorAccent}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PrimaryButtonStyle -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ColorAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="{StaticResource FontMain}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ColorAccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DangerButtonStyle -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource ColorDanger}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CardBorderStyle -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ColorSurface}"/>
            <Setter Property="CornerRadius" Value="{StaticResource RadiusCard}"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColorBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- DataGridStyle (light theme) -->
        <Style x:Key="DataGridDarkStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource ColorSurface}"/>
            <Setter Property="RowBackground" Value="{StaticResource ColorSurface}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource ColorSurfaceHover}"/>
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource ColorBorder}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource ColorAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EEF2FF"/>
                    <Setter Property="Foreground" Value="{StaticResource ColorAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- InputBoxStyle -->
        <Style x:Key="InputBoxStyle" TargetType="Control">
            <Setter Property="Background" Value="{StaticResource ColorSurface}"/>
            <Setter Property="Foreground" Value="{StaticResource ColorTextPrimary}"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontFamily" Value="{StaticResource FontMain}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource InputBoxStyle}">
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="PasswordBox" BasedOn="{StaticResource InputBoxStyle}">
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ColorAccent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox" BasedOn="{StaticResource InputBoxStyle}"/>
        <Style TargetType="DatePicker" BasedOn="{StaticResource InputBoxStyle}"/>

        <!-- ValidationErrorTemplate -->
        <ControlTemplate x:Key="ValidationErrorTemplate">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource ColorDanger}" FontSize="16" FontWeight="Bold" Margin="5,0,0,0" ToolTip="{Binding ElementName=AdornedElement, Path=AdornedElement.(Validation.Errors)[0].ErrorContent}">!</TextBlock>
                <Border BorderBrush="{StaticResource ColorDanger}" BorderThickness="1">
                    <AdornedElementPlaceholder Name="AdornedElement"/>
                </Border>
            </DockPanel>
        </ControlTemplate>

    </Application.Resources>
</Application>
