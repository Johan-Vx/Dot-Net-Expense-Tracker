<UserControl x:Class="Expense_Tracker.View.DashBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expense_Tracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tổng quan hệ thống" FontSize="28" FontWeight="Bold" Foreground="{StaticResource ColorTextPrimary}" Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1: Tổng số dư -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="TỔNG SỐ DƯ TẤT CẢ QUỸ" Foreground="{StaticResource ColorTextMuted}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding TongSoDu, StringFormat={}{0:N0} VNĐ}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ColorSuccess}"/>
                </StackPanel>
            </Border>

            <!-- Card 2: Thu tháng này -->
            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="TỔNG THU THÁNG NÀY" Foreground="{StaticResource ColorTextMuted}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding TongThuThangNay, StringFormat={}{0:N0} VNĐ}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ColorAccent}"/>
                </StackPanel>
            </Border>

            <!-- Card 3: Chi tháng này -->
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="TỔNG CHI THÁNG NÀY" Foreground="{StaticResource ColorTextMuted}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding TongChiThangNay, StringFormat={}{0:N0} VNĐ}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ColorDanger}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Recent Transactions -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Giao dịch gần đây" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                
                <DataGrid ItemsSource="{Binding RecentTransactions}" Style="{StaticResource DataGridDarkStyle}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding SoPhieu}" Width="100"/>
                        <DataGridTextColumn Header="Ngày Lập" Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Loại" Binding="{Binding LoaiPhieu}" Width="100"/>
                        <DataGridTextColumn Header="Số Tiền" Binding="{Binding TongTien, StringFormat={}{0:N0}}" Width="150"/>
                        <DataGridTextColumn Header="Người Lập" Binding="{Binding NguoiDung.HoTen}" Width="150"/>
                        <DataGridTextColumn Header="Nội Dung" Binding="{Binding LyDoChung}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

    </Grid>
</UserControl>
